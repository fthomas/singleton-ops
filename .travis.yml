language: scala
sudo: required
scala:
 - 2.11.12
 - 2.12.6
 - 2.13.0-M4
jdk:
 - oraclejdk8
cache:
  directories:
   - "$HOME/.ivy2/cache"
   - "$HOME/.sbt/boot/"
script:
 - travis_wait 60 sbt ++$TRAVIS_SCALA_VERSION validate
after_success:
 - bash <(curl -s https://codecov.io/bash)
